import React from 'react';
import ReactDOM from 'react-dom/client';
import TresLuas from './tres-luas-app';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TresLuas />
  </React.StrictMode>
);
